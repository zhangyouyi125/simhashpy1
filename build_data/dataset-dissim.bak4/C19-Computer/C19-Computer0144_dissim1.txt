计算机应用
COMPUTER APPLICATIONS
1999年 第19卷 第10期 Vol.19 No.10 1999



JDK 1.1的多语种支持
路骏
　　JDK 1.1的改变主要影响了三个包：
　　Java.util, 增加Locale类。Locale 类封装了局域的一些信息，如国家,语种,日期代码等。但并不提供实际的局域化操作。如果没有指定一个Locale, JDK1.1会从环境中提取缺省的Locale(us-en)。
　　2） 运行速度较快
　　采用多层结构，数据获取、数据处理都在中间层的SAS应用服务器上执行，处理完的信息才返回浏览器端，减少了频繁的网络数据传送，提高了前端的响应速度。他们往往只能定期地将程序重复运行，将分析的结果（如图片和表格）静态地公布，从而使决策分析在时间和费用上增加支出。一方面，通过数据服务可以查询数据集并按用户请求返回一定格式的数据；另一方面，通过计算服务可以使用户能在浏览器上对数据进行分析，调用SAS软件的过程和函数功能。每一个原有的在类一层的输入输出字节流，现在都有对应的Reader和Writer类，用字符流完成相似的功能。
覃英华　主要从事信息服务系统开发。JDK 1.1提供了InputStreamReader和OutputWriter两个类，用来创建基于字节流的新的Reader和Writer对象，使Reader/Writer类同目前使用字节流的其他类兼容。
3.3　系统逻辑结构

3.4　功能实现
　　以下是通过SAS的应用程序发布机在浏览器上动态生成报表的一个实例，在浏览器实现对数据表进行列间运算、表间合并、旋转，生成所需要的报表动态地返回到浏览器端。他提供了处理各种文本的支持，包括文本排序，格式化日期与数值，以及分析相关语言数据。
　　应用JDK 1.1提供的新方法，可以将程序中地区化的标签，文本和处理地区化信息的源程序封装到要支持的每一个Locale对应的ResourceBundle对象中，只要把指定的Locale源转译成新的语言或习惯，就可以实现移植。
2　SAS的Web应用技术
　　SAS/IntrNet 为SAS 的Web应用提供数据服务和计算服务